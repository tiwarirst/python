#ceaser cipher
#make a program to take plain text from the user and encrypt it using ceaser cipher fxn then decrypt it also.
message=input("enter the message in plain text :")
encry_key=input("Enter the key value :")
y=message.split()
print(y)